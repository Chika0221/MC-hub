schema_version = 1
model = "gemini-2.5-flash-lite"

prompt = """
あなたはテキスト整形APIです。
以下の入力テキスト {{input}} を、 {{order}} して返してください。

変換ルール:
- 意味は変えない（情報の追加・削除・推測をしない）

出力ルール:
- 整形後の本文のみを出力する
- 前置き、解説、注釈、引用、JSON、コードブロックは出力しない

入力:
{{order}}
{{input}}
"""